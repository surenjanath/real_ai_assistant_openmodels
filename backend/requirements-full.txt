# Optional extras on top of requirements.txt, for a fully provisioned host.
# The backend auto-detects what is importable and degrades gracefully.

# Reference torch implementation of Kokoro (Python <3.13 only). pykokoro in
# requirements.txt is preferred and faster; this is the fallback adapter.
kokoro>=0.9 ; python_version < "3.13"
soundfile

# --- Phase 5: agentic stack ----------------------------------------------------
crewai>=0.80
# FastMCP tool server + n8n bridge (backend/tools/mcp_server.py)
mcp>=1.2
httpx
